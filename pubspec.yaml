name: web_deploy_poc
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: "^3.3.0"

dependencies:
  bloc: ^8.1.3
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.4
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.1
  # using alpha version of flutter_html to fix compile issue: 
  # https://github.com/Sub6Resources/flutter_html/issues/1398
  flutter_html: ^3.0.0-alpha.3
  go_router: ^13.1.0
  url_launcher: ^6.2.4
  http: ^1.2.0
  equatable: ^2.0.5




dev_dependencies:
  bloc_test: ^9.1.6
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true
  generate: true
